import SwiftUI

struct OpenSourceLicensesView: View {
    var body: some View {
        Text("Open Source Licenses Content Goes Here")
            .navigationBarTitle("オープンソースライセンス", displayMode: .inline)
    }
}
